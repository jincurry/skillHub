apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: skillhub-data
  namespace: skillhub
  labels:
    app.kubernetes.io/name: skillhub
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
