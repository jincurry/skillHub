apiVersion: v1
kind: Service
metadata:
  name: skillhub
  namespace: skillhub
  labels:
    app.kubernetes.io/name: skillhub
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: skillhub
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
